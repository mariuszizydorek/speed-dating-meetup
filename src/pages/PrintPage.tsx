import { Container, Typography } from '@mui/material';

export function PrintPage() {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4" component="h1">Print</Typography>
    </Container>
  );
}
